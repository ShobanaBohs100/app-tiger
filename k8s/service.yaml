apiVersion: v1
kind: Service
metadata:
  name: chaos-app
  namespace: sre-prototype
spec:
  selector:
    app: chaos-app
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
  type: ClusterIP